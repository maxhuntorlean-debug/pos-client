function documentListPage({id,title,columns}){return`<main class="sale-page document-page"><button id="${id}BackButton" class="back-button">← Назад</button><div class="saleHeader"><h2 class="saleTitle">${title}</h2></div><table class="saleTable"><thead><tr>${columns.map(c=>`<th>${c}</th>`).join("")}</tr></thead><tbody></tbody></table></main>`}
export function showReport(app,showHome){app.innerHTML=documentListPage({id:"report",title:"Отчет продаж",columns:["ДОК","ТОВАР","ПРОД"]});document.getElementById("reportBackButton").addEventListener("click",showHome)}
export function showLog(app,showHome){app.innerHTML=documentListPage({id:"log",title:"Журнал событий",columns:["Дата","Событие","Описание"]});document.getElementById("logBackButton").addEventListener("click",showHome)}
